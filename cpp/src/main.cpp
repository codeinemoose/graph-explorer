#include "./include/main.h"
#include <string>

int main(int const argc, char* argv[]){
    std::string command;
    if(argc > 1){
        command = argv[1];
    }
    
    if(command == "process"){

    }
}